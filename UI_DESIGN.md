# زبان طراحی اورجینال «اتاق فرمان ملی»

نسخه ۶ رابط کاربری را از مجموعه‌ای از فرم‌ها و نقشه‌های جدا به یک تجربه نقشه‌محور تبدیل می‌کند. این طراحی کپیِ رابط هیچ بازی نیست؛ الگوهای موفق مطالعه و با نیاز شبیه‌ساز فارسی، موبایل، RTL و موتور اتمی پروژه بازطراحی شده‌اند.

## مطالعه مرجع

### Victoria 3
- تیم هنری Victoria 3 بر سلسله‌مراتب بصری، واضح‌بودن نقاط تصمیم و آرام‌بودن نقشه برای رقابت‌نکردن با UI تأکید کرده است:
  - https://www.paradoxinteractive.com/games/victoria-3/news/dev-diary-49-graphic-overview
- بهبودهای UX آن شامل Map Mode، Heatmap خواناتر، اطلاعات زمینه‌ای و پنل تعامل مستقیم نقشه است:
  - https://www.paradoxinteractive.com/games/victoria-3/news/dev-diary-74-ux-improvements
- نتیجه برای پروژه: نقشه پس‌زمینه تصمیم است، نه تزئین؛ لایه داده و اقدام زمینه‌ای باید کنار همان مکان باشند.

### Civilization VI و CQUI
- جامعه CQUI موفقیت رابط را با «اطلاعات لازم با کلیک کمتر»، لنزهای نقشه، بنرهای هوشمند و Tooltip غنی دنبال می‌کند:
  - https://github.com/Azurency/CQUI_Community-Edition
- نتیجه برای پروژه: شاخص‌های ملی همیشه در نوار بالا، جزئیات در Hover/Touch، و اقدام‌های پرتکرار مستقیم در پنل انتخاب نقشه.

### Frostpunk
- رابط کنسولی Frostpunk زبان دایره‌ای خود را از ساختار واقعی شهر گرفته و گزینه‌های ضروری را در دسترس انگشت نگه می‌دارد:
  - https://news.xbox.com/en-us/2019/09/20/frostpunk-console-edition-radial-driven-design-of-gameplay-and-controls/
- نتیجه برای پروژه: زبان بصری باید از مفهوم «اتاق فرمان جغرافیایی» بیاید؛ فیروزه‌ای برای داده/کنترل، طلایی برای انتخاب و قرمز برای خطر.

### افشای تدریجی
- Progressive Disclosure اطلاعات ضروری را ابتدا نشان می‌دهد و جزئیات را بر اساس نیاز/زوم آشکار می‌کند:
  - https://ixdf.org/literature/topics/progressive-disclosure
- نتیجه برای پروژه: جهان ← منطقه ← کشور/استان ← شهر/شبکه ← جزئیات محلی، بدون صفحه نقشه دوم.

## معماری تجربه

### یک نقشه، پنج سطح معنایی
1. **جهان**: مرز کشورها، روابط و کریدورهای راهبردی.
2. **منطقه**: نام کشورهای بیشتری آشکار می‌شود.
3. **کشور و استان**: Admin-1 کشور انتخابی و Heatmap منطقه‌ای.
4. **شهر و شبکه**: شهرها، پایتخت، جاده، ریل، فرودگاه و بندر.
5. **جزئیات محلی**: نام نواحی و شاخص‌های دقیق‌تر.

زوم پیوسته است و به مرکز اشاره‌گر انجام می‌شود. Drag، Wheel، Pinch، Pan Gesture، دوبار لمس، کلیدهای +/−، Home و 0 پشتیبانی می‌شوند.

### لنزهای داده
- سیاسی، روابط، جمعیت، اقتصاد، زیرساخت، رضایت، امنیت، اقلیم، منابع و نظامی.
- Overlayهای مستقل جنگ، اتحاد، تجارت، پرواز، دریا، زمین، شهر، راه/ریل و اطلاعات.
- رنگ هر ناحیه برای کشور بازیکن از State زنده و برای دیگر کشورها از پروفایل همان کشور محاسبه می‌شود.

### اقدام زمینه‌ای
- کشور خارجی: بهبود رابطه، تجارت، اتحاد، تحریم، جنگ و صلح.
- کشور بازیکن/استان: نگهداری راه، زهکشی، برف‌روب، نمک جاده، رزمایش زمستانی و مراکز خنک‌کننده.
- مسیر: نوع، ظرفیت و ریسک اختلال.
- همه تصمیم‌ها همچنان از Command و تراکنش اتمی GameEngine عبور می‌کنند.

## زبان بصری

- پس‌زمینه اورجینال رویه‌ای، بدون تصویر خارجی: گرادیان عمیق، شبکه مختصات و درخشش کم.
- پنل‌های نیمه‌شفاف با حاشیه سرد، گوشه ۸ تا ۱۲ پیکسل و سایه کنترل‌شده.
- **فیروزه‌ای**: تعامل و اطلاعات؛ **طلایی**: انتخاب و اقدام مهم؛ **قرمز**: خطر؛ **سبز**: وضعیت سالم.
- نوار فرمان ثابت: کشور، تاریخ، GDP، رضایت، ثبات و هشدار.
- ناوبری افقی قابل اسکرول برای جلوگیری از بریدگی در موبایل.
- داک عملیات با یک اقدام اصلی واضح: «اجرای ماه بعد».
- حداقل ارتفاع کنترل‌های پرتکرار ۴۴ پیکسل و پشتیبانی از بزرگ‌نمایی متن/کنتراست.

## تجربه حرفه‌ای نسخه ۶.۲

- **فرمان سریع:** جست‌وجوی فارسی یکپارچه میان ۱۱ مرکز، ۶۵ سامانه و ۱۹۵ کشور با Ctrl+K/F1 و دسترسی لمسی.
- **پنل زمینه‌ای پایدار:** انتخاب کشور، استان یا مسیر فقط Context را عوض می‌کند؛ Map Node و دوربین دیگر بازسازی نمی‌شوند.
- **واکنش‌گرایی:** در عرض کم یا متن بزرگ، شاخص‌ها ۲ ستونه، Overlayها ۳ ستونه و نقشه متناسب می‌شود.
- **اعلان انباشته:** چهار Toast دارای شدت، ورود/خروج کنترل‌شده و حالت بدون حرکت.
- **ریزتعامل:** Fade پله‌ای صفحات، Inertia نقشه، صدای رویه‌ای و لرزش اختیاری Android.
- **دسترس‌پذیری:** پالت کوررنگی، کنتراست، تراکم راحت/فشرده، چهار اندازه متن، توقف حرکت و خاموش‌کردن Tooltip.
- **نمودار تعاملی:** Crosshair، انتخاب ماه با Hover/Touch/Keyboard، مقدار دقیق چهار سری و ناحیه رنگی.
- **میانبر:** Alt+عدد برای مراکز اصلی و Ctrl+K برای فرمان سریع.

## خوانایی و لمس نسخه ۶.۳

- تایپوگرافی پایه از ۱۷ به ۲۸ واحد طراحی افزایش یافت؛ حالت فشرده نیز حداقل ۲۴ واحد دارد.
- عنوان‌ها، ردیف‌ها، دکمه‌ها، شاخص‌ها، Toast، فرمان سریع و نوشته‌های خود نقشه جداگانه بزرگ‌سازی شدند.
- `TouchScrollContainer` برای محتوای عمودی، نوار ناوبری، داک عملیات و نتایج جست‌وجو Drag و Inertia واقعی فراهم می‌کند.
- کنترل‌های تعاملی نقشه اسکرول والد را مسدود می‌کنند تا کشیدن نقشه با اسکرول صفحه تداخل نداشته باشد.
- Pinch نقشه مستقیماً دو `InputEventScreenTouch` را دنبال می‌کند؛ فاصله دو انگشت Zoom و جابه‌جایی مرکز آن‌ها Pan را کنترل می‌کند.
- Touch-to-mouse در تنظیمات پروژه صریح است تا دکمه‌ها روی Android قابل لمس بمانند.
- نمای معمول تلفن ۱۰۸۰ پیکسل به‌صورت پیش‌فرض از چیدمان دو ستونه و Overlay سه‌ستونه استفاده می‌کند.

## پایداری و ضدهنگ نسخه ۶.۳.۱

- مسیر UI از `tick_async` استفاده می‌کند و ۲۹ تا ۳۱ روز داخلی را بین فریم‌ها Yield می‌کند؛ تراکنش و نتیجه با مسیر همگام دترمینستیک یکسان است.
- Overlay مسدودکننده، روز جاری و ProgressBar را نشان می‌دهد و فرمان تکراری را تا Commit نمی‌پذیرد.
- نقشه هنگام Drag، Pinch و Inertia وارد LOD سبک می‌شود: Triangulation، Admin-1، مسیرها، شهرها و Labelهای سنگین موقتاً متوقف می‌شوند.
- ۱۷۰ میلی‌ثانیه پس از توقف دوربین، رندر کامل فقط یک بار بازسازی می‌شود.
- Auto-turn در زمان اجرای ماه متوقف است و Save/همگام‌سازی تا پایان زیر Overlay انجام می‌شود.
- Immersive Mode اندروید خاموش است و دکمه Back حتی هنگام محاسبه اجازه خروج فوری می‌دهد؛ بازی دیگر نباید کاربر را روی صفحه محبوس کند.

## اصالت و محدودیت

تمام اجزای بصری با Godot Draw API، Control و StyleBoxFlat داخل پروژه ساخته شده‌اند. هیچ تصویر، Icon Pack، Tile Server، CDN یا کد رابط بازی‌های مرجع استفاده نشده است. داده نقشه همچنان آفلاین و دارای مجوز Public Domain است.

## بازطراحی نسخه ۷ — «اتاق فرمان» با زبان HOI4/EU4

نسخه ۷ چیدمان کروم را از «فرم‌های عمودی» به چیدمان بازی‌های استراتژی بزرگ بازسازی کرد. الگوهای مرجع (نوار قدرت HOI4، پنجره‌های زمینه‌ای EU4، داک زمان Frostpunk) حفظ و با محدودیت موبایل عمودی و RTL ترکیب شدند.

### اجزای نویین کروم

- **نوار بالایی حکومتی:** نشان ملی، نام کشور، تاریخ و فرمان سریع با خط طلایی زیرین؛ چهار شاخص حیاتی (اقتصاد، رضایت، ثبات، هشدار) به‌صورت چیپ‌های فشرده با نقطه رنگی — لمسی و همیشه دیده‌شونده.
- **ناوبری پایانی ثابت:** ۵ بخش اصلی (نقشه ◉، میز فرمان ▦، دولت ♜، اقتصاد ◈، بیشتر ☰) با نشانگر طلایی زیر آیتم فعال؛ جایگزین نوار افقی اسکرول‌شونده نسخه ۶.
- **کشوی «بیشتر» (Bottom Sheet):** هفت بخش فرعی (قوانین ⚖، توسعه ⚒، فناوری ⚛، جامعه ☺، دفاع ⚔، چندنفره ◍، سامانه‌ها ⚙) به‌همراه ذخیره/بارگذاری/صدا؛ با پس‌زمینه کم‌نور و بستن با Back یا لمس بیرون.
- **داک زمان:** «ماه بعد ▶» طلایی حکومتی به‌عنوان اقدام اصلی + جریان خودکار و سرعت؛ الگوی مستقیم HOI4 در دسترس شست.
- **تیکر رخدادها:** نوار باریک زنده؛ لمس آن میز فرمان را باز می‌کند.

### تب نقشه راهبردی

- **کارت قهرمان آغاز:** نشان ❖ طلایی، عنوان درشت، انتخاب کشور/سناریو در ردیف‌های برچسب‌دار و CTA بزرگ؛ جایگزین کارت ساده نسخه ۶.
- **چیپ‌های لنز:** ده لنز تحلیلی (سیاسی تا نظامی) به‌صورت پیل گرد تک‌انتخابی با حالت فعال فیروزه‌ای؛ جایگزین OptionButton.
- **پیل‌های لایه:** جنگ/اتحاد/تجارت/... با چراغ وضعیت طلایی؛ جایگزین CheckButton.
- **ستون شناور دوربین:** ✚ − ⌂ ⊙ ◐ روی خود نقشه؛ دسترسی بدون ترک نمای راهبردی.
- **نشان موضع کشور:** پیل رنگی (متحد/دوستانه/خنثی/خصمانه/در جنگ) + دکمه‌های خطر/موفقیت برای جنگ و پاسخ پیشنهادها.

### سیستم رنگ و نشان

پالت «نیلیِ شب + طلایی حکومتی» با لهجه‌های فیروزه/آبی/سبز/قرمز؛ نشان‌های تک‌رنگ یونیکد (◉ ▦ ♜ ◈ ☰ ⚖ ⚒ ⚛ ⚔ ✓ ✕ ▶ ⚡) که روی همه پلتفرم‌ها بدون فونت اضافی رندر می‌شوند و سبک آیکون فلت بازی‌های مرجع را تداعی می‌کنند.

### اسکرین‌شات‌های نسخه ۷

| صفحه | تصویر |
|---|---|
| آغاز فرماندهی (قهرمان) | `docs/screenshots/ui_v2_home_hero.png` |
| نقشه فرماندهی + ستون دوربین | `docs/screenshots/ui_v2_command_map.png` |
| میز فرمان + کارت‌های KPI | `docs/screenshots/ui_v2_dashboard.png` |
| کشوی بیشتر | `docs/screenshots/ui_v2_drawer.png` |

## پوسته گرافیکی نسخه ۷.۱ — گذار به «بازی گرافیکی»

- **هنر کلیدی اختصاصی (AI، رایگان و متعلق به پروژه):** بنر سینمایی «زمین در شب با مسیرهای تجاری» و نشان طلایی «عقاب و کره» (در `assets/art`) برای صفحه آغاز فرماندهی؛ نشان با حذف رنگ پس‌زمینه شفاف‌سازی و هیچ‌کدام از منابع خارجی/پولی استفاده نشده است.
- **لایه افکت زنده نقشه** (`scripts/ui/map_fx_layer.gd`): موج نور اقیانوس در نمای جهان، پالس حلقه‌ای ⚔ روی کشورهای در جنگ، جریان متحرک دشت‌های طلایی و کاروان در مسیرهای تجارت، ابرهای شناور در لنز اقلیم و چراغ ★ پایتخت بازیکن. بازترسیم حداکثر ~۱۲ فریم‌برثانیه، توقف کامل با `reduce_motion` و در حالت LOD حرکتی؛ هیچ اثری روی State یا دترمینیسم موتور ندارد.
- **رندر سینمایی ایستا:** سایه ساحلی برای برجسته‌شدن خشکی از اقیانوس، هاله آبی اختصاصی کشور بازیکن، هاله طلایی نرم پشت مرز کشور انتخابی و استخر نور ملایم در اقیانوس — همه بدون هزینه فریم اضافه.
- **تپش تنفسی** روی دکمه‌های کلیدی قهرمان (CTA شروع فرماندهی).
- جریان بازی و APIهای نقشه/تست نگهبان بدون تغییر ماند؛ شمار رویدادهای تست بلندمدت (۴۹۵۴) بایت‌به‌بایت ثابت مانده است.

## بازسازی گرافیکی نسخه ۸ — «نقشه دائمی تمام‌صفحه»

نسخه ۸ پرش از «رابط با نقشه» به «بازی روی نقشه» است؛ الگوی HOI4/EU4 که نقشه زنده بوم اصلی است و پنل‌ها HUD شناور روی آن:

- **نقشه جهان دائمی**: نقشه یکپارچه یک‌بار در `map_wrap` تمام‌صفحه ساخته می‌شود و در همه برگه‌ها پشت کروم زنده می‌ماند؛ برگه نقشه پرده را برمی‌دارد و خروجی، پان و پینچ مستقیم به نقشه می‌رسد.
- **HUD شناور**: نوار KPI، داک زمان (ماه بعد/خودکار/سرعت) و ناوبری پایانی نیمه‌شفاف روی نقشه می‌نشینند؛ برگه نقشه فضای ریشه را بین «فاصله‌ساز بالا» (نمای نقشه) و «برگه پایین» نصف می‌کند و برگه با `ALIGNMENT_END` به پایین می‌چسبد و اسکرول می‌خورد.
- **مُدال قهرمان آغاز**: بنر «زمین در شب»، نشان عقاب-کره، انتخاب کشور/سناریو و دکمه «⚑ شروع فرماندهی» روی نقشه جهانِ زنده؛ حین محاسبه ناهمگام نخستین ماه، قهرمان بازسازی نمی‌شود تا پرده پیشرفت روزبه‌روز دیده شود و دوباره‌زنی دکمه شروع ممکن نشود. کلیدها هم در حالت مُدال مسدودند.
- **کارت «گزارش جبهه و دیپلماسی»**: جنگ‌های جاری، پیشنهادهای ورودی، پیمان‌ها و تحریم‌ها — جریان سیاست جهان در یک نگاه روی نقشه.

### اسکرین‌شات‌های نسخه ۸
| صحنه | فایل |
| --- | --- |
| مُدال قهرمان آغاز روی نقشه جهان زنده | `docs/screenshots/ui_v8_hero_map.png` |
| نقشه تمام‌صفحه + HUD و برگه شناور پایین صفحه | `docs/screenshots/ui_v8_map_hud.png` |
