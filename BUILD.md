# راهنمای ساخت و اجرا - شبیه‌ساز کشور

## اجرای محلی (Godot)

1. نصب Godot 4.2.2 از https://godotengine.org
2. کلون ریپو: `git clone https://github.com/paraday960/Game.git`
3. باز کردن پروژه در Godot: فایل `project.godot` را باز کنید
4. اجرای صحنه اصلی: F5 یا دکمه Play

## ساخت اندروید محلی

1. نصب Android SDK + JDK 17
2. در Godot: Editor > Export > Android
3. تنظیم Keystore (debug)
4. Export → `build/country-sim.apk`

## ساخت خودکار اندروید (GitHub Actions) - توصیه شده ✅

طبق `GUIDE_ANDROID.md` و قانون ۵ (فقط رایگان):

- هر `push` روی `main` → Workflow اجرا می‌شود
- Godot + Templates خودکار نصب می‌شوند
- APK در `Releases` قرار می‌گیرد
- کاملا رایگان برای ریپوی public

### مشاهده وضعیت ساخت:
- برو به تب Actions در گیت‌هاب
- آخرین Workflow را ببین
- APK را از Artifacts یا Releases دانلود کن

## معماری موتور

### اجرای اتمی (۳.۶)
```
ورودی → اعتبارسنجی → Snapshot → محاسبه همه سیستم‌ها روی کپی → Commit یا Rollback
```
- هیچ وضعیت نیمه‌کاره دیده نمی‌شود
- ضدتقلب و ضد Desync

### لایه‌های معماری (۳.۷)
1. Atomic
2. Idempotency (کلید یکتا برای هر فرمان)
3. Versioning (نسخه‌بندی خوش‌بینانه)
4. Deterministic Lockstep (seed مشترک)
5. Event Sourcing (گزارش رویدادها)
6. Command-Based Tick (فقط فرمان ارسال می‌شود)

### بدون سرور مرکزی (۳.۸)
- ارتباط واقعی و رایگان با `ENetMultiplayerPeer` در حالت میزبان/کلاینت مستقیم
- کلاینت فقط فرمان می‌فرستد؛ محاسبه و Commit روی میزبان مرجع انجام می‌شود
- Snapshot نسخه‌دار همراه SHA-256 برای همگام‌سازی کلاینت‌ها
- محدودسازی تعداد و اندازه فرمان دریافتی؛ بازگشت امن به تک‌نفره در قطع اتصال
- اتصال اینترنتی مستقیم نیازمند بازکردن/Port Forward پورت میزبان است؛ LAN بدون سرویس پولی کار می‌کند

### سیستم‌ها (۶۵ سامانه متصل)
- ۳۳ سیستم عملکردی کشور، لایه اماکن فیزیکی، مردم و سیستم‌های تکمیلی همگی در `system_order` اجرا می‌شوند.
- هر سیستم فایل، فرمول، رویداد و وضعیت مستقل دارد؛ تست ۳۶۵روزه نبود NaN/Inf را کنترل می‌کند.
- مرکز پایش UI برای تک‌تک ۶۵ سامانه، سلامت و شاخص‌های تفصیلی نمایش می‌دهد.
- مقادیر کلیدی از `data/balance.json` و وضعیت آغازین از `data/initial_state.json` خوانده می‌شوند.

### هوش اختصاصی (۲.۲.۷)
- هر ۶۵ سامانه AI و پروفایل هدف اختصاصی دارد.
- `AIAdvisor` سلامت، فوریت و دلیل فارسی را محاسبه و پیشنهاد بودجه قابل اجرا تولید می‌کند.
- AI کشور بازیکن فقط پیشنهاد می‌دهد؛ `build_autonomous_commands` برای کشورهای غیرِبازیکن فعال است.

### فارسی‌سازی (قانون ۶)
- تمام UI فارسی
- `PersianFormatter`: اعداد فارسی ۰-۹، واحدهای فارسی (میلیارد ریال)
- متن‌ها، پیام‌ها، رویدادها فارسی

### پیشرفت و درگیری بازیکن (۳.۲۲۷-۳.۲۳۴)
- بازخورد فوری هر تیک و رویدادهای متغیر چندگزینه‌ای
- پنج مرحله توسعه از دولت نوپا تا ابرقدرت
- ۱۰ دستاورد واقعی، استریک، شتاب تا ×۵، XP، سطح، رکورد و امتیاز میراث
- تاریخچه تصمیم‌ها و داستان نوظهور ناشی از پیامدهای چندسیستمی

## تکنولوژی (قانون ۵ - فقط رایگان)
- Godot 4.2.2 - MIT
- GDScript
- GitHub Actions - رایگان برای public repo
- Vazirmatn Font - OFL
- همه متن‌باز و رایگان ✅

## وضعیت قابلیت‌ها
- [x] ۶۵ سامانه شامل ۳۳ سیستم کشور، مردم، اماکن و لایه‌های تکمیلی
- [x] نقشه جهان تعاملی آفلاین با Natural Earth Public Domain
- [x] هوش تخصصی مردم/سامانه‌ها و شورای پیشنهاددهنده
- [x] ۱۱ دسته اماکن فیزیکی و مرکز پایش تفصیلی
- [x] چندنفره مستقیم واقعی با ENet (میزبان مرجع، فرمان‌محور، Snapshot امضاشده)
- [x] ساخت APK، Artifact و انتشار خودکار در Releases
- [ ] کشف خودکار اینترنتی/NAT traversal رایگان در کنار اتصال مستقیم
